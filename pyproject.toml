[build-system]
requires = ['setuptools>=68']
build-backend = 'setuptools.build_meta'

[project]
name = 'scholarly-manuscript-revision-agent'
version = '0.2.0'
description = 'Deterministic domain models for source-grounded scholarly manuscript revision'
requires-python = '>=3.11'
dependencies = [
    'pydantic>=2.0,<3',
    'PyYAML>=6.0,<7',
]

[project.optional-dependencies]
dev = [
    'pytest>=8,<10',
]

[tool.setuptools]
package-dir = {'' = 'src'}

[tool.setuptools.packages.find]
where = ['src']

[tool.pytest.ini_options]
testpaths = ['tests']
addopts = '-ra'
